---
import Layout from "../layouts/Layout.astro";
import Footer from "../components/Footer.astro";
---

<Layout>
  <!-- Content -->
  <div class="text-center p-4 bg-base-100 text-base-content h-screen shadow-lg">
    <h3 class="mb-4 antialiased text-3xl font-semibold">Requirements</h3>
    <div class="mb-2 flex items-center justify-center">
      <div role="alert" class="alert alert-info w-full lg:w-3/7 mx-auto">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="h-6 w-6 shrink-0 stroke-current">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <span>Major Linux distributions that support systemd.</span>
      </div>  
    </div>
    <div class="mb-2 flex items-center justify-center">
      <div role="alert" class="alert alert-warning w-full lg:w-3/7 mx-auto">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 shrink-0 stroke-current" fill="none" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
        </svg>
        <span>Please install systemd and awk,<a class="link" href="https://code.visualstudio.com/Download#" target="_blank">vscode</a></span>
      </div>  
    </div>
    <h3 class="mb-4 antialiased text-3xl font-semibold">Installation</h3>
    <p class="mb-2">Go on then, paste and run the following in a terminal:</p>
    <div class="mb-2 mockup-code w-full lg:w-3/7 mx-auto">
      <pre data-prefix="$" class="text-xs lg:text-base text-left"><code>curl -s "https://rising3.github.io/code/get/" | bash</code></pre>
    </div>
    <h3 class="mb-4 antialiased text-3xl font-semibold">Uninstallation</h3>
    <p class="mb-2">Go on then, paste and run the following in a terminal:</p>
    <div class="mb-2 mockup-code w-full lg:w-3/7 mx-auto">
      <pre data-prefix="$" class="text-xs lg:text-base text-left"><code>systemctl --user stop code</code></pre>
      <pre data-prefix="$" class="text-xs lg:text-base text-left"><code>systemctl --user disable code</code></pre>
      <pre data-prefix="$" class="text-xs lg:text-base text-left"><code>rm -f ~/.config/systemd/usr/code*</code></pre>
    </div>
  </div>  
  <!-- Content -->

  <!-- Footer -->
   <Footer/>
   <!-- Footer -->
</Layout>
